@model IEnumerable<PDKBattleTracker.Models.Faction>

@{
    ViewData["Title"] = "Index";
}

<h1>Factions</h1>

<p>
    <a asp-area="" asp-controller="Game" asp-action="Index">Back </a>
    |
    <a asp-action="Create"> Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FactionName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FactionName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FactionId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FactionId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FactionId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
