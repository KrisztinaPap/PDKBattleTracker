@model IEnumerable<PDKBattleTracker.Models.GameSystem>

@{
    ViewData["Title"] = "Index";
}

<h1>Game Systems</h1>

<p>
    <a asp-area="" asp-controller="Game" asp-action="Index">Back </a>
    |
    <a asp-action="Create"> Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.GameSystemName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.GameSystemName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.GameSystemId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.GameSystemId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.GameSystemId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
