@model IEnumerable<PDKBattleTracker.Models.GameType>

@{
    ViewData["Title"] = "Index";
}

<h1>Game Types</h1>

<p>
    <a asp-area="" asp-controller="Home" asp-action="Admin">Back </a>
    |
    <a asp-action="Create"> Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.GameTypeName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.GameTypeName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.GameTypeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.GameTypeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.GameTypeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
